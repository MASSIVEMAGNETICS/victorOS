# A1 Historical Reconciliation

**Status:** evidence cross-reference for A1-RL-001  
**Authority:** descriptive only; this file does not change Victor canon.

A1 is not a greenfield invention. The relevant episode/artifact history contains several overlapping ancestors that must remain distinct by evidence status.

## 1. Personal Intelligence Runtime / Bando PIC — 2026-08-08

PIC proposed a local-first personal intelligence runtime with:

- source/entity/claim/relation layers
- append-only hash-chained event history
- opportunity detection and deterministic scoring
- next-action selection
- outcome learning
- integrity verification
- mapping onto Chronos, Rograph, TRACE-0, Ethica and related Victor primitives

The preserved PIC episode explicitly states that its runtime, CLI, and unit tests were **not executed** in that chat environment. Treat PIC as prior architecture/design evidence, not current verified runtime state.

A1 is intentionally narrower than PIC. A1 performs structural recurrence, contradiction, provenance and episodic-history analysis. If PIC or an equivalent decision runtime is revived, A1 should become one analysis module behind shared evidence/transition interfaces rather than expand into a competing operating system.

## 2. Resonance Microscope v0.1 — 2026-08-18

The Resonance Microscope is prior runnable evidence-first digital-cymatics work. Its core discipline is directly inherited by A1:

- apparent structure is not automatically meaningful
- recurrence must be tested
- null/negative evidence must survive
- conclusions must remain narrower than the measurement supports

A1 translates that epistemic discipline from physical/time-series signals into historical concept recurrence. It does **not** reuse spectral statistics as semantic truth scores.

## 3. Computational Chladni Lens v0.1

The CCL research line formalized cross-transform testing, matched nulls, persistence and held-out utility. Its main contribution to A1 is methodological: multiple views can strengthen evidence, but no visualization or resonance score is a probability of truth.

## 4. Architecture Genealogy v1.0.0 — frozen 2026-08-24

The frozen genealogy establishes:

```text
TRACE-0 -> INFORMATRON -> CHRONOS -> REDUCER -> ROGRAPH/WORLD MODEL
```

and the `ExperienceTransition` causal-accounting seam. It also records explicit failure modes including duplicate Chronos and canon without verification.

Therefore A1's local hash-linked event log is an **A1 receipt chain only**. It must never be presented as the Chronos canonical head. Future promotion requires a verified one-way adapter that emits candidate `ExperienceTransition` records through existing authority and verification gates.

## 5. 2026-08-30 consolidation

The current consolidation adds the rule:

```text
Resonance != Truth
```

and the Chladni systems lens:

```text
stable recurring structure -> nodal invariant candidate
high-change recurring structure -> active resonant mode
single-source structure -> experiment
contradiction -> preserved pressure, never averaged away
```

A1 requires at least three independent sources before it can label a concept `NODAL_INVARIANT_CANDIDATE`, and at least two for `ACTIVE_RESONANT_MODE`.

## Reconciled architecture

```text
PRIVATE / EXTERNAL EPISODE SOURCES
              |
              v
        A1-RL ANALYZER
  recurrence / contradiction
  provenance / concept graph
              |
              v
  CANDIDATE ExperienceTransition
              |
       authority + verification
              |
              v
           CHRONOS
              |
           REDUCER
              |
      ROGRAPH / WORLD MODEL
              |
     PIC / DECISION RUNTIME
      (when verified/revived)
```

This preserves the useful parts of every ancestor without creating another canonical truth store.
